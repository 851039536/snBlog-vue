/**
 * 是否展示加载框
 */
export const aspShow = ref(false)

/**
 * 选中后变色并且效果不消失
 */
export const sideIndex = ref(-1)
//login
export const hLogin = ref(false)
export const hSearch = ref(false) //是否显示搜素框
export const hSearchValue = ref('') //搜素事件是否存在
