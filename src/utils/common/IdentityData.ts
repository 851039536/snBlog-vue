/**
 * 是否展示加载框
 */
export const loadingVisible = ref(false)

/**
 * 选中后变色并且效果不消失
 */
export const sideIndex = ref(-1)
//login
export const loginVisible = ref(false)
export const searchVisible = ref(false) //是否显示搜素框
/**
 *  是否显示顶部栏
 */
export const headVisible = ref(true)
/**
 *  是否显示侧边栏
 */
export const sideVisible = ref(true)
